local config = MaxScoreboard.config

--------------------------------------------------
-- Edit these to configure the scoreboard for your server.
--------------------------------------------------

-- Text shown in the header instead of a logo image. "highlight" is drawn
-- in its own color (config.communityNameHighlightColor, client-side),
-- "rest" follows it in the normal header text color.
config.communityNameHighlight = "MAX"
config.communityNameRest = " Servers DarkRP"
